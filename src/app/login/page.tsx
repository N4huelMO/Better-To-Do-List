"use client";
import { Main } from "@/styles/sharedStyles";
import styled from "styled-components";

const Login = () => {
  return (
    <Main>
      <div>Login</div>
    </Main>
  );
};

export default Login;
